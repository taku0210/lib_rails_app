<h1>ログイン</h1>

<%= form_with(scope: :session, url: login_path, local: true) do |f| %>
  <div class="field">
    <%= f.label :student_no, "学籍番号" %><br>
    <%= f.text_field :student_no %>
  </div>

  <div class="field">
    <%= f.label :password, "パスワード" %><br>
    <%= f.password_field :password %>
  </div>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
<% end %>

<p>アカウントをお持ちでないですか？ <%= link_to "新規登録", signup_path %></p>